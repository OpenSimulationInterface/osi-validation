DetectedEntityHeader:
  measurement_time:
    - is_set
  cycle_counter:
    - is_set
  data_qualifier:
    - is_set

SensorData:
  version:
    - is_set!
  timestamp:
    - is_set
  sensor_id:
    - is_set
  mounting_position:
    - is_set
  mounting_position_rmse:
    - is_set
  sensor_view:
    - is_set
# TODO: rules for the other attributes?