TrafficLight:
  id:
    - is_global_unique
  base:
    - is_set
  classification:
    - is_set
  Classification:
    color:
      - is_set
    icon:
      - is_set
    mode:
      - is_set
    counter:
      - is_minimum: 0
    assigned_line_id:
      - refers: Lane
    is_out_of_service:
      - is_set