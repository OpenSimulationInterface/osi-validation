SensorView:
  version:
    - is_set!
  timestamp:
    - is_set
  sensor_id:
    - is_global_unique
  mounting_position:
    - is_set
  mounting_position_rmse:
    - is_set
  host_vehicle_data:
    - is_set
  global_ground_truth:
    - is_set
  host_vehicle_id:
    - is_set
    - refers: 'MovingObject'