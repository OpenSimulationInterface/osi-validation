Vector3d:
  x:
    - is_set
  y:
    - is_set
  z:
    - is_set

Vector2d:
  x:
    - is_set
  y:
    - is_set

Dimension3d:
  length:
    - is_set
    - is_minimum: 0
  width:
    - is_set
    - is_minimum: 0
  height:
    - is_set
    - is_minimum: 0

Orientation3d:
  roll:
    - is_set
  pitch:
    - is_set
  yaw:
    - is_set

MountingPosition:
  position:
    - is_set
  orientation:
    - is_set

Spherical3d:
  distance:
    - is_set
    - is_minimum: 0
  azimuth:
    - is_set
  elevation:
    - is_set

BaseStationary:
  dimension:
    - is_set
  position:
    - is_set
  orientation:
    - is_set
  base_polygon:
    #- is_set

BaseMoving:
  dimension:
    - is_set
  position:
    - is_set
  orientation:
    - is_set
  velocity:
    - is_set
  acceleration:
    - is_set
  orientation_rate:
    - is_set
  orientation_acceleration:
    #- is_set
  base_polygon:
    #- is_set

Identifier:
  value:
    - is_minimum: 0
Timestamp:
  seconds:
    - is_minimum: 0
  nanos:
    - in_range: [0, 999999999]