DetectedTrafficSign:
  header:
    - is_set
  main_sign:
    - is_set
  DetectedMainSign:
    base:
      - is_set
    base_rsme:
      - is_set
    geometry:
      - is_set
    CandidateMainSign:
      probability:
        - in_range: [0, 1]
  DetectedSupplementarySign:
    base:
      - is_set
    base_rsme:
      - is_set
    CandidateSupplementarySign:
      probability:
        - in_range: [0, 1]